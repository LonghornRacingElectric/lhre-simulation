# DS-009 FSAE Vehicle Dynamics Design Notebook

Run from `BobSim` after generating the simulation reports:

```bash
python tools/generate_fsae_design_notebook.py
```

The canonical report is `reports/DS-009-fsae-design-notebook.md`. Rendered figures are in `plots/`.
