# AERO-001 Map And Reference Audit

This is the aero gatekeeper. It does not claim that the aero package is good;
it only verifies the map can be interpreted safely.

Run with:

```bash
python studies/aero/AERO-001-map-and-reference-audit/run.py
```
